<div class="theme-pricing {{type}}">
    <div class="content title"><h3>{{title}}</h3></div>
    <div class="content price"> {{price}} </div>
    <div class="content yield"> {{yield}} </div>
    {{#if buttonAction}}
        <div class="content button"><button>{{buttonAction}}</button></div>
    {{/if}}
</div>
