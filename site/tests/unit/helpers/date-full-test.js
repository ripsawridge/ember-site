import { dateFull } from 'mountainwerks/helpers/date-full';
import { module, test } from 'qunit';

module('Unit | Helper | date full');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateFull([42]);
  assert.ok(result);
});
