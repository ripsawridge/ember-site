import { dateMmyyyy } from 'mountainwerks/helpers/date-mmyyyy';
import { module, test } from 'qunit';

module('Unit | Helper | date mmyyyy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateMmyyyy([42]);
  assert.ok(result);
});
