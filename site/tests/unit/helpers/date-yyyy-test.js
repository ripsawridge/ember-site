import { dateYyyy } from 'mountainwerks/helpers/date-yyyy';
import { module, test } from 'qunit';

module('Unit | Helper | date yyyy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateYyyy([42]);
  assert.ok(result);
});
